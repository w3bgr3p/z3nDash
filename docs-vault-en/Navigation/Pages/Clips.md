# Clips

Clips stores named text templates for quick copying.

## Capabilities

- browse the template tree;
- select and copy content;
- create and edit entries;
- store a path, a title and the text.

## Storage

The data lives in the `clips` table.

## API

- `GET /clips`
- `POST /clips`
