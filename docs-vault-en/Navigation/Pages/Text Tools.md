# Text Tools

The Text page holds local text transforms.

## Tools

- URL encode/decode;
- Base64 encode/decode;
- C# string escaping;
- JSON string escaping.

Everything runs in the browser and needs no database.
